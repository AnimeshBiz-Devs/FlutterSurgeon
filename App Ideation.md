A flutter driven app that works like Figma/PenPot - but everything we draw is bounded by code.
For example,
We start by choosing standard - SafeArea, Scaffold etc. as a base

Then we get buttons to add a Stack, Column, Row etc. 
We can also alter their properties as per we want - change width to fill (maybe using media-query), fixed. Adding padding values too. It's like the programs PenPot and Figma, but we are tightly bounded by the tools. Consider Framer. How we have Stack Layout that let's us add a Horizontal or Vertical Stack Layout. Then converts to code. Same Mechanic.

Then also allow us to add buttons etc. And according to the design it generates a code. Only for UI purpose. The gradient, text style, etc. it can easily accomodate and generate the code for